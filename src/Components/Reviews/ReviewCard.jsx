

const ReviewCard = ({ review }) => {
    const { review_id,
        username,
        user_image,
        review_rating,
        review_text } = review
    return (
        <div>

        </div>
    );
};

export default ReviewCard;